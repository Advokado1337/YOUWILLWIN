spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/youwillwin
    username: youwillwin
    password: youwillwin
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080
